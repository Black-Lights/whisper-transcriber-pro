# Whisper Transcriber Pro - Requirements (Stable Production Versions)
# Author: Black-Lights (https://github.com/Black-Lights)
# Project: Whisper Transcriber Pro v1.2.0

# Install with: pip install -r requirements.txt

# Core dependencies (Stable, well-tested versions)
openai-whisper>=v20240930
tqdm>=4.67.1
numpy>=2.0.2
requests>=2.32.3
psutil>=7.0.0

# PyTorch (Stable LTS-like versions)
# Note: The installer will automatically choose GPU or CPU version
torch>=2.7.1
torchaudio>=2.7.1

# Audio/Video processing
ffmpeg-python>=0.2.0

# GUI dependencies (included with Python)
# tkinter (built-in)

# Development dependencies (optional - uncomment if needed)
# pytest>=8.4.0
# black>=25.1.0
# flake8>=7.2.0

# Note: GPU/CPU versions are handled by the installer
# GPU Support (CUDA 11.8) - Applied automatically by installer if GPU detected
# CPU Support - Applied automatically by installer if no GPU detected